You are an expert C/C++ and OpenMP programmer specializing in high-performance parallel computing. Your task is to translate the following serial C/C++ code into an equivalent **maximally optimized** parallel version using OpenMP directives (`#pragma omp ...`).

**STRICT RULES — follow them without exception:**

1. The observable behavior of the program **MUST** be identical to the serial version (same output, same results, same side effects).
2. Include `#include <omp.h>` if it is not already present.
3. Return **ONLY** the translated source code — no explanations, no markdown fences, no comments outside the code itself.

**OPTIMIZATION MANDATE — apply every applicable OpenMP technique to extract maximum performance:**

4. **Loop parallelism:** Parallelize every loop that has no true data dependency using `#pragma omp parallel for`. For nested loops, evaluate whether `collapse(N)` can flatten the iteration space for better load distribution.
5. **Schedule tuning:** Choose the optimal `schedule` clause for each loop based on its workload pattern:
   - `static` for uniform iterations.
   - `dynamic` or `guided` for irregular/variable-cost iterations.
   - Justify your choice by the structure of the loop body.
6. **Reductions:** Use `reduction(op:var)` for all accumulation patterns instead of `critical` or `atomic` when possible, as reductions scale better.
7. **Minimize synchronization:** Prefer `atomic` over `critical`; prefer `reduction` over `atomic`. Avoid barriers (`#pragma omp barrier`) unless strictly required. Use `nowait` on `for` and `sections` constructs wherever the following code does not depend on the loop result.
8. **Data-sharing clauses:** Explicitly declare every variable as `private`, `firstprivate`, `lastprivate`, `shared`, or `threadprivate` as appropriate. **Never** rely on OpenMP's default sharing rules — use `default(none)` and declare every variable.
9. **SIMD vectorization:** Where the compiler can benefit, add `simd` clauses (`#pragma omp simd` or `#pragma omp parallel for simd`) on innermost loops with simple arithmetic and no cross-iteration dependencies.
10. **Task parallelism:** For recursive algorithms, producer–consumer patterns, or irregular workloads that don't map well to loop parallelism, use `#pragma omp task` / `#pragma omp taskloop` with appropriate `depend` clauses.
11. **Memory & cache optimization:**
    - Use `#pragma omp parallel for` with data-locality-aware access patterns.
    - Consider padding or aligning shared arrays to avoid false sharing.
    - If applicable, use `#pragma omp allocate` with the appropriate allocator.
12. **Nested parallelism:** If the code has nested parallel regions that can benefit from it, enable with `omp_set_nested(1)` or `omp_set_max_active_levels()`, but **only** if it demonstrably improves performance.
13. **Thread count:** Do **not** hardcode thread counts. Let the runtime decide, or use `num_threads()` only if there is a clear justification.
14. **Minimize parallel region overhead:** Prefer a single `#pragma omp parallel` region with internal `#pragma omp for` / `#pragma omp sections` work-sharing constructs rather than opening and closing multiple parallel regions.
15. **Critical-path reduction:** Identify the critical path and ensure maximum concurrency on it. Move serial-only code (I/O, initialization) outside parallel regions.

**Serial code to translate:**